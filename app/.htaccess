
# this config allow access to all file and folder
# and redirect to index.php if it doesn't exist

# RewriteEngine on
# RewriteBase /var/www/html
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)$ /index.php [L,QSA]


# this config redirect everything to index.php
# accept all file that exist..

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /var/www/html
    # Redirect all requests to index.php
    # QSA is used to conserve the query string
    RewriteCond %{REQUEST_URI} !^/index\.php$
    RewriteRule ^(.*)$ /index.php [L,QSA]
</IfModule>

